# Connected apps and MCP compatibility

This legacy filename is retained so older internal links remain valid. Follow
`references/data-sources.md` for the current capability-based workflow. Do not assume
provider-specific configuration files, local MCP servers, fixed tool names, or local credential
storage. Never request secrets in chat.
